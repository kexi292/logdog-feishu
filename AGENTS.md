# Logdog Feishu 开发约束

## 目标

- Linux 上常驻运行，监听多个项目、多个服务和多个日志路径。
- 一个实例只使用一个飞书群自定义机器人 Webhook。
- 日志告警必须包含项目、服务、监听目录和实际日志文件路径。
- 服务器资源告警包含主机、指标、阈值和持续条件，复用同一个飞书 Webhook。
- 优先低内存、少依赖、单二进制部署。

## 实现规则

- 使用 Go 标准库优先；只有明确需要时才增加依赖。
- 不开发飞书应用机器人、私聊、个人用户路由、多 Webhook 或常驻 Web 管理后台。
- Webhook、签名密钥等敏感信息不得写入代码、示例配置、日志或测试输出。
- 日志读取必须处理游标持久化、日志轮转、文件删除和优雅退出。
- HTTP 请求必须使用 TLS 校验、超时、响应状态检查和有限重试。
- 配置格式变更要同步更新示例、校验和文档。

## 开发与验证

```bash
go test ./...
go vet ./...
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o logdog-feishu .
```

- 新增非平凡逻辑时补最小可运行测试。
- 提交前确认没有真实 Webhook、secret 或生产日志。
- 使用功能分支，通过 Pull Request 合并；不要直接推送原始上游仓库或受保护分支。
