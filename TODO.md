# Logdog Feishu 开发清单

目标：发现日志异常后主动发送带来源、原文和上下文的报告，减少每日人工巡检，由维护者自行排查和修复应用。开发约束见 [AGENTS.md](AGENTS.md)，当前用法和限制见 [README.md](README.md)。

## 本轮收口

- [x] 统一本仓库 Go 模块路径；使用标准库完成轮询、报告采集、状态保存和 HTTP 发送。
- [x] 多项目、服务、路径共用一个 Webhook，报告包含实际目录、文件身份和字节范围。
- [x] 恢复游标、文件替换/截断/删除、新文件发现、半行续读及退出收尾。
- [x] 同一文件保留前置窗口、堆栈和后文；有限规则无法确定边界时说明完整性，超长内容明确标注。
- [x] 长报告分段、编码大小检查、TLS、超时、HTTP/业务状态检查、有限重试和共享发送间隔。
- [x] 待发报告在原状态文件中落盘，成功后确认；失败退出后可重发，即使原日志已删除。
- [x] TUI 菜单动作状态修正、凭据遮蔽、非法扫描周期拒绝保存；配置权限与原子写入。
- [x] 补充临时日志与本地 HTTPS 行为测试，覆盖重启、文件变化、上下文、分段和失败路径。
- [x] 删除失效的需求草稿和手写消息模板，README 与示例只描述当前版本。
- [x] 完整测试、race 检查、vet 与 Linux amd64 静态构建通过。
- [x] 提供 amd64/arm64 静态安装包、SHA-256 校验、systemd 示例和逐步部署说明；构建产物不提交。

## 接下来只做两项

1. **完成菜单体验**：可见的保存/退出确认、删除服务、路径可读性检查、报告预览；长路径与小终端适配。关闭 TUI 后不常驻。
2. **Linux 验收**：按部署说明验证 systemd 启动、网络恢复、日志轮转、实际 RSS 和吞吐。真实群消息仅在用户明确选择发送测试消息时发送。

## 保留的验收边界

- [x] 临时日志测试覆盖两个项目、每个项目两个服务、每个服务两个路径，规则和状态互不串用。
- [ ] 在实际 Linux 上验证长时间运行、多服务持续写入、错误风暴和网络故障。
- [ ] 真实飞书群验证格式、分段、签名及平台额度；不能用本地替身结果代替真实验收。
- [ ] 发布前确认上游代码授权，再决定整个项目的许可证；当前不声明派生项目已获 MIT 授权。

## 暂不做

不引入数据库、消息队列、DI 框架、Web 后台、跨服务链路关联或自动修复部署。先保留单进程、单 Webhook、同步发送和本地状态文件；只有实测吞吐不足才考虑更复杂的发送调度。

Go 技能源码、项目入口和审查记录仅供本地开发，通过 `.git/info/exclude` 排除；不属于产品依赖或发布文件。
