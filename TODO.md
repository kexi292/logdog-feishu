# Logdog Feishu 改造 TODO

## 当前结论

- 继续使用 Go 标准库完成日志读取、匹配和 HTTP 发送。
- 保留 `gopkg.in/yaml.v3`；TUI 采用一个成熟的 Go 终端 UI 库并检查其传递依赖，暂不引入日志框架、文件监听库或飞书 SDK。这些库在构建时编译进程序，服务器无需安装。
- 一个应用实例只配置一个飞书群 Webhook，所有项目和服务的告警都发送到该群。
- 人员变更通过维护群成员完成，不配置个人用户 ID，不开发飞书应用机器人。
- 所有改造先保证 Linux 单机、低内存、可长期运行；生产服务器不要求安装 Go。
- 产品目标是代替每日人工查看日志：发现异常后主动发送带排查上下文的报告，由维护者判断、修复和更新应用。

本文是后续开发清单，以下配置设计和未勾选能力尚未实现。

## 飞书通知结论

[自定义机器人使用指南](https://open.feishu.cn/document/ukTMukTMukTM/ucTM5YjL3ETO24yNxkjN)确认：群 Webhook 支持单向推送，无需创建应用或维护 token，满足当前告警需求。它绑定一个群，更换接收群时更新地址即可。

本次查阅自定义机器人文档的限制为单租户单机器人 100 次/分钟、5 次/秒，请求体不超过 20 KB；实现时以官方文档为准，同时约束分钟和秒级速率。多个服务共用一个 Webhook 时应共享额度。

不维护个人 @ 名单。Webhook 地址和签名密钥按凭据保存，不出现在版本库、告警或运行日志中。

## 多项目、多服务配置设计（待实现）

一个进程读取一份配置，沿用现有 `inputs` 列表。每项用 `project` 标识项目、`name` 标识服务，项目和服务名组合唯一；`paths` 可配置多个日志目录、文件或 glob 规则。所有服务共用顶层唯一的 `output.http`，服务级别不配置接收目标。

以下是目标配置草案，当前程序尚未支持全部字段。为简化使用，允许在权限为 `0600` 的本地配置文件中填写 Webhook 和可选签名密钥；真实配置不提交 Git。配置界面可生成同一文件，采用按需启动的终端交互界面（TUI）。

```yaml
output.http:
  url: https://open.feishu.cn/open-apis/bot/v2/hook/REPLACE_ME
  secret: ""
inputs:
  - project: mall
    name: order-api
    paths:
      - /var/log/order
      - /opt/order/logs/error.log
    include_lines: ['ERROR', 'Exception']
    exclude_lines: ['KnownHealthCheckError']
  - project: mall
    name: payment-worker
    paths:
      - /var/log/payment/*.log
    include_lines: ['ERROR', 'FATAL']
  - project: operations
    name: scheduler
    paths:
      - /var/log/scheduler/*.log
    include_lines: ['ERROR']
```

目录路径默认匹配当前目录的 `*.log`，不递归；文件和 glob 沿用 `filepath.Glob` 语义，不承诺 `**` 递归。每条告警保留命中的配置路径规则、对应监听目录以及实际文件绝对路径。glob 中的目录通配符应解析为本次触发文件的实际目录，并同时展示原始规则。Webhook、项目名或服务名等必要配置缺失时启动报错；修改配置后重启生效，先不做热加载。

例如订单服务的一条消息应包含：

```text
项目：mall
服务：order-api
主机：prod-01
监听规则：/var/log/order
监听目录：/var/log/order
日志文件：/var/log/order/app.log
触发时间：2026-09-11T10:30:00+08:00
关键字：ERROR
日志内容：ERROR database connection timed out
```

## 当前代码与目标的差距

`input/keeper.go` 已经遍历多个 `inputs` 启动监听，`input/input.go` 已支持多文件/glob 和包含/排除关键字，但会跳过目录路径。`Input.read` 调用 `Publisher.Write` 时只传日志字节，丢失项目、服务、规则和文件路径。保留 `app/app.go` 中的全局单一发送器，补齐目录展开和来源信息即可。

## 第一阶段：修复现有骨架

- [ ] 保留顶层字面键 `output.http:` 和 `inputs`；增加项目标识时提供旧配置迁移说明。
- [ ] 修复飞书配置示例与代码不一致的问题：当前示例使用 `${...}`，发送代码只替换 `%{content}`。
- [ ] 校验项目名、服务名、日志路径、匹配关键字和唯一 Webhook；默认使用 POST 和内置消息格式，用户无需填写请求头或 JSON 模板。
- [ ] 修复 HTTP 请求错误处理：检查响应状态码、关闭响应体、设置超时，记录失败原因。
- [ ] 检查飞书响应体中的业务 `code`，HTTP 200 不代表发送成功；修复 JSON 模板中数组/数字被断言为对象而导致的 panic，并正确转义日志内容。
- [ ] 修复请求头解析：避免无空格或格式错误的 header 导致数组越界。
- [ ] 删除不可达代码，确保 `go vet ./...` 无警告。
- [ ] 为多项目配置补充最小自检，确认各服务独立扫描、匹配并保留来源信息。
- [ ] 支持一次启动监听多个项目和服务：每个服务独立配置项目名、服务名、监听目录/文件 glob、包含关键字和排除关键字，共用同一 Webhook。
- [ ] 每条告警带上项目名、服务名、配置的监听目录和实际触发日志文件绝对路径，便于多服务排查。

## 第二阶段：日志读取正确性

- [ ] 按项目名、服务名、日志绝对路径和文件身份维护游标；同一路径被多个服务配置引用时不串用处理进度。
- [ ] 启动时从游标继续读取，首次发现文件时默认从文件末尾开始，避免重复发送历史日志。
- [ ] 游标持久化到可配置的本地状态文件；写入采用临时文件加 rename，避免进程中断损坏。
- [ ] 处理日志轮转：检测 inode 或文件大小回退，关闭旧文件并从新文件起点继续。
- [ ] 处理文件删除、重建和 glob 新增文件，避免旧文件描述符泄漏。
- [ ] 读取循环使用 ticker 或可唤醒机制，退出时能够关闭文件和 goroutine。
- [ ] 为超长日志行设置上限，避免单条日志占用无限内存。

## 第三阶段：告警内容和发送策略

- [ ] 告警事件包含：项目名、服务名、主题、主机名、时间、触发关键字、命中的监听规则、监听目录、实际日志绝对路径和上下文。
- [ ] 告警报告保留异常事件原文，包括多行堆栈、嵌套原因（如 `Caused by`）和已有的请求/追踪标识；事件前后各 5 行作为补充，不以 5 行为限截断堆栈。
- [ ] 上下文只从同一服务、同一文件身份收集，保留原始顺序和可定位的字节范围；相邻日志不等于同一请求，不做跨文件或跨服务的自动关联。
- [ ] 根据日志事件边界收集堆栈；边界无法识别、等待后文超时、达到采集上限或文件中断时，报告明确标注上下文完整性未知或未完整，并注明原因，不把固定行数窗口称为完整上下文。
- [ ] 长报告按统一报告编号和分段序号发送，每段包含来源信息，按 JSON 编码后的请求体大小检查平台限制；达到报告或发送容量上限时明确提示未发送的内容及原日志位置，不静默截断。
- [ ] 上下文采集使用有界缓冲和有界等待；日志不再追加时仍能发出已有报告。覆盖长堆栈、半行分次写入、连续命中、日志停写、轮转和长消息分段的最小测试。
- [ ] 一条日志命中多个关键字时只发送一条告警，并列出全部命中关键字。
- [ ] 支持包含关键字和排除关键字的明确优先级：命中排除关键字时不告警。
- [ ] 增加可选去重/节流窗口，避免错误风暴打满飞书接口。
- [ ] 支持 Webhook 签名 secret；本地配置限制文件权限，URL 和 secret 不写入日志和错误信息；移除仓库示例中的真实 Webhook 地址。
- [ ] 所有服务共用单一 Webhook、有界发送队列和限流额度；队列满时明确记录丢弃或合并数量。
- [ ] 对飞书限流和临时网络错误做有限重试，避免无限堆积内存。

## 第四阶段：Linux 运行方式

- [ ] 提供 Linux amd64/arm64 构建，按服务器 CPU 架构设置 `GOARCH`，使用 `CGO_ENABLED=0 GOOS=linux go build`；验证无动态链接依赖。
- [ ] 明确部署方式：Go 只在开发机或 CI 中使用；服务器只上传编译后的 Linux ELF 二进制，不执行 `go run`。
- [ ] 说明程序只需要向飞书发起 HTTPS 出站请求，不需要监听 HTTPS 入站端口；优先使用 Linux 发行版已安装的系统 CA 信任库，精简系统缺少 CA 时补装 `ca-certificates`，不能关闭 TLS 验证。
- [ ] 提供示例 `config.yaml`，不包含真实 Webhook、secret 或用户标识。
- [ ] 提供 systemd service 示例，包含工作目录、配置路径、自动重启和最小权限运行说明。
- [ ] 增加 `-version`、`-config` 参数和清晰的启动错误信息。
- [ ] 增加优雅退出：处理 SIGINT/SIGTERM，保存游标并关闭文件。
- [ ] 在实际 Linux 上分别测量后台监听与 TUI 的 RSS，覆盖空闲、多服务持续写入、错误风暴和网络失败；记录日志数量与吞吐，验证常驻内存有界，不用二进制体积代替内存指标。

## 验收标准

- [ ] `go test ./...` 通过，`go vet ./...` 无警告。
- [ ] 一次启动监听至少两个项目、每个项目至少两个服务、每个服务至少两个日志路径；各服务匹配规则、来源字段和游标互不串用。
- [ ] 每条告警包含项目名、服务名、对应监听目录、原始配置路径规则和触发日志文件的绝对路径。
- [ ] 所有服务发到同一个 Webhook；更换人员通过维护告警群成员完成，无需改代码。
- [ ] 重启程序不会重复告警已经处理过的日志行。
- [ ] 日志轮转后新文件能够继续告警，旧文件不会持续占用文件描述符。
- [ ] 飞书 Webhook 收到格式正确且来源完整的消息；编码后的请求体在平台大小限制内，共用 Webhook 时不突破总速率限制。
- [ ] 超过 5 行的异常堆栈在采集上限内完整保留，长报告可按编号顺序阅读；上下文不足或超出上限时告警明确说明原因及原日志位置。
- [ ] 网络不可用时程序不会崩溃或无限制增长内存。

## 配置界面与应用形态

部署目标为一个 Linux Go 可执行文件，由 systemd 管理后台监听。配置界面只在用户操作时启动，关闭后不常驻。TUI 和监听器共用一个二进制，通过子命令区分。以下命令是待实现设计，当前尚不可用。

| 形态 | 操作方式 | 成本与边界 |
| --- | --- | --- |
| TUI 配置工具 | `logdog-feishu configure -c /etc/logdog/config.yaml`；SSH 中编辑 Webhook、项目、服务、路径和关键字 | 按需启动独立进程，关闭后退出；保存配置后重启监听服务生效 |
| 后台监听命令 | `logdog-feishu run -c /etc/logdog/config.yaml`，由 systemd 启动 | 无需终端，不启动 TUI 事件循环，负责持续监听和发送告警 |

两种命令共用同一配置文件，不维护额外数据库。TUI 库会增加二进制体积，也可能带来初始化开销；只在配置命令中创建界面对象，不承诺对后台内存零影响，以实测为准。

- [ ] TUI 支持一个 Webhook 和可选签名密钥的编辑、多个项目/服务的增删改、每个服务多条路径和包含/排除关键字的编辑。
- [ ] 提供路径可读性检查、配置校验和告警内容预览；只有用户选择“发送测试消息”时才向群发消息。
- [ ] 敏感字段默认隐藏；保存前校验，使用临时文件加 rename 和 `0600` 权限；退出未保存时提示，写入失败保留原配置。
- [ ] SSH 中全键盘操作，兼容小终端和无颜色环境；无 TTY 时配置命令明确报错，后台监听仍可启动。

## 暂不做

- 不开发飞书应用机器人、私聊、个人用户路由和多 Webhook。
- 不开发临时 Web 页面、Web 管理后台或另一套 CLI 配置向导；不引入 Kafka、Redis 或数据库。
- 暂不支持 Prometheus/Alertmanager 全套链路；需要时单独增加适配器。
- 暂不支持复杂规则表达式，先使用包含/排除关键字即可。
